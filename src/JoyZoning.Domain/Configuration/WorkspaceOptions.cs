namespace JoyZoning.Domain.Configuration;

/// <summary>Workspace execution options (JSDP uses canonical session root only).</summary>
public sealed class WorkspaceOptions
{
    public const string SectionName = "Workspace";
}
